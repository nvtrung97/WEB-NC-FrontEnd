import React from 'react';
import PropTypes from 'prop-types';

CourseReview.propTypes = {
  course: PropTypes.object,
};

function CourseReview(props) {
  return <div>course review</div>;
}

export default CourseReview;
